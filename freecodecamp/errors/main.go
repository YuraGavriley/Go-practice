package main

/*

import "fmt"

func notMain() {
	user, err := getUser()
	if err != nil {
		fmt.Println(err)
		return
	}
	profile, err := getUserProfile(user.ID)
	if err != nil {
		fmt.Println(err)
		return
	}
	// user.useUserHere()
}

func getUser() (User, error) {
	// some get user logic here
}

*/
